# WinMax
WinMax is a lightweight macOS utility that replaces the native green-button fullscreen behavior with Windows-style maximize and restore. It keeps windows on the normal desktop, allows other windows to appear above them, and avoids separate fullscreen Spaces.
